# Pi config

Mostly stolen from https://github.com/davis7dotsh/my-pi-setup.

- **Skills:** background terminals, `PLAN.md` tracking, and subagent delegation.
- **Extensions:** background processes (`/ps`), Firecrawl web tools, subagents (`/subagents`, `/btw`), and multi-agent workflows (`/workflows`; request “ultracode”).
- Ask naturally or use `/skill:<name>` explicitly; `/reload` picks up configuration changes.
